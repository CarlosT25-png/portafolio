import startLogo from '../../assets/start-menu-logo.png';

const StartBtn = () => {
  return (
    <img
      src={startLogo}
      alt="Windows XP Start Menu - Web"
    />
  );
};

export default StartBtn;
